diff --git a/wan/image2video.py b/wan/image2video.py
--- a/wan/image2video.py
+++ b/wan/image2video.py
@@
     def generate(self,
                  input_prompt,
                  img,
                  action_path,
@@
                  image_condition_file=None,
                  dump_image_condition=None,
                  latents_file=None,
-                 output_latents_file=None,):
+                 output_latents_file=None,
+                 latent_chunk_sink=None,):
@@
             image_condition_file=image_condition_file,
             dump_image_condition=dump_image_condition,
             latents_file=latents_file,
-            output_latents_file=output_latents_file)
+            output_latents_file=output_latents_file,
+            latent_chunk_sink=latent_chunk_sink)
@@
     def _generate_causal_fast(self,
@@
                               image_condition_file=None,
                               dump_image_condition=None,
                               latents_file=None,
-                              output_latents_file=None,):
+                              output_latents_file=None,
+                              latent_chunk_sink=None,):
@@
         seed = seed if seed >= 0 else random.randint(0, sys.maxsize)
+        stream_started = time.perf_counter()
+        generation_id = f"{seed}-{time.time_ns()}"
         seed_g = torch.Generator(device=self.device)
@@
                 pred_latent_chunks.append(x0)
+
+                # Optional progressive-generation seam. Default None keeps the
+                # existing hot path free of CPU copies and I/O.
+                if latent_chunk_sink is not None:
+                    from wan.streaming.events import LatentChunkEvent
+                    event = LatentChunkEvent(
+                        generation_id=generation_id,
+                        chunk_index=chunk_id,
+                        total_chunks=num_inference_chunk,
+                        latent_start=chunk_id * chunk_size,
+                        latent_count=int(x0.shape[1]),
+                        shape=tuple(int(v) for v in x0.shape),
+                        dtype=str(x0.dtype),
+                        seed=seed,
+                        elapsed_ms=(time.perf_counter() - stream_started) * 1000.0,
+                    )
+                    latent_chunk_sink.on_latent_chunk(event, x0)
 
                 # Update kv cache
@@
     def _generate_causal_pretrain(self,
@@
                                   seed=-1,
                                   offload_model=True,
                                   max_sequence_length=512,
-                                  max_attention_size=None,):
+                                  max_attention_size=None,
+                                  latent_chunk_sink=None,):
@@
         seed = seed if seed >= 0 else random.randint(0, sys.maxsize)
+        stream_started = time.perf_counter()
+        generation_id = f"{seed}-{time.time_ns()}"
         seed_g = torch.Generator(device=self.device)
@@
                 pred_latent_chunks.append(current_latent)
+
+                if latent_chunk_sink is not None:
+                    from wan.streaming.events import LatentChunkEvent
+                    event = LatentChunkEvent(
+                        generation_id=generation_id,
+                        chunk_index=chunk_id,
+                        total_chunks=num_inference_chunk,
+                        latent_start=chunk_id * chunk_size,
+                        latent_count=int(current_latent.shape[1]),
+                        shape=tuple(int(v) for v in current_latent.shape),
+                        dtype=str(current_latent.dtype),
+                        seed=seed,
+                        elapsed_ms=(time.perf_counter() - stream_started) * 1000.0,
+                    )
+                    latent_chunk_sink.on_latent_chunk(event, current_latent)
 
                 # Update both self KV caches with the clean latent (once for cond, once for uncond)
